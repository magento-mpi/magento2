<div class="box-right">
    <h4 class="icon-head head-customer-sales-statistics"><?=__('Sales Statistics')?></h4>
    <div class="grid">
        <table cellspacing="0" class="data">
            <thead>
                <tr class="headings">
                    <th><?=__('Store')?></th>
                    <th><?=__('Lifetime Sale')?></th>
                    <th><?=__('Average Sale')?></th>
                </tr>
            </thead>
            <tfoot>
                <tr>
                    <td class="label"><strong><big><?=__('All Stores')?></big></strong></td>
                    <td class="emph"><strong><big><?=$this->getPriceFormatted($this->getTotals()->getLifetime())?></big></strong></td>
                    <td class="emph"><strong><big><?=$this->getPriceFormatted($this->getTotals()->getAvgsale())?></big></strong></td>
                </tr>
            </tfoot>
            <tbody>
                <?$_i = 0;?>
                <?foreach ($this->getRows() as $_row):?>
                    <tr<?=($_i++ % 2 ? ' class="even"' : '')?>>
                        <td class="label"><?=$_row->getStoreName()?></td>
                        <td><?=$this->getPriceFormatted($_row->getLifetime())?></td>
                        <td><?=$this->getPriceFormatted($_row->getAvgsale())?></td>
                    </tr>
                <?endforeach;?>
            </tbody>
        </table>
    </div>
</div>