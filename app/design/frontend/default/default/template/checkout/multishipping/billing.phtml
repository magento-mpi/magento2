<?
/**
 * Multishipping checkout billing information
 * 
 * @see Mage_Checkout_Block_Multishipping_Billing
 */
?>
<div class="page-head">
    <h3>Billing Information</h3>
</div>
<?=$this->getMessagesBlock()->getGroupedHtml()?>
<script src="<?=Mage::getBaseUrl(array('_type'=>'js'))?>varien/payment.js" type="text/javascript"></script>
<form action="<?=$this->getPostActionUrl()?>" method="post" id="multishipping-billing-form">
<div class="multi-address-checkout-box">
    <div class="box">
        <div class="content col2-set">
            <div class="col-1">
                <div class="head">
                    <h5>
                        Billing Address<span class="pipe">|</span><a href="<?=$this->getSelectAddressUrl()?>">Change</a>
                    </h5>
                 </div>
                <?$_address=$this->getAddress()?>
                <address>
                    <?=$_address->getFormated(true)?>
                </address>
            </diV>
            <div class="col-2">
                <div class="head">
                    <h5>Payment Method</h5>
                </div>
                <div class="tool-tip" id="payment-tool-tip" style="left:0;position:absolute;display:none">
                    <a href="#" id="payment-tool-tip-close" class="btn-close"><img src="<?=$this->getSkinUrl('images/btn_window_close.gif')?>" alt="Close"/></a>
                    <div class="block-content">
                    <img src="<?=$this->getSkinUrl('images/cvv.gif')?>" alt="Card Verification Number visual reference"/>
                    </div>
                </div>
                <dl class="payment-methods">
                    <?=$this->getPaymentMethods()?>
                </dl>
            </div>
        </div>
      </div>
</div>
<div class="form-buttons">
    <a href="<?=$this->getBackUrl()?>" class="left">&laquo; Back to Shipping Information</a>
    <button class="form-button" type="submit"><span>Continue to Place Order</span></button>
</div>
<script type="text/javascript">
    // Need to fix payment JS
    var payment = new paymentForm('multishipping-billing-form');
    function toggleToolTip(event){
        if($('payment-tool-tip')){
            $('payment-tool-tip').setStyle({
                top: (Event.pointerY(event)-500)+'px',
                left: (Event.pointerX(event)-800)+'px'
            })
            $('payment-tool-tip').toggle();
        }
        Event.stop(event);
    }
    if($('payment-tool-tip-close')){
        Event.observe($('payment-tool-tip-close'), 'click', toggleToolTip);
    }
    $$('.cvv-what-is-this').each(function(element){
        Event.observe(element, 'click', toggleToolTip);
    });
</script>