<?if($poll) :?>
<?$_poll = $poll->getPoll();?>
<div class="box mini-poll">
	<div class="head"> <!-- Gives room for control buttons -->
		<h3><span>Today's Poll</span></h3>
	</div>
	<h4><?=$_poll['poll_title'];?></h4>
    <?if( is_array($poll->getAnswers()) ):?>
	    <table cellspacing="0">
		    <tbody>
            <?foreach( $poll->getAnswers() as $answer ):?>
                <tr>
                    <td><?=$answer['answer_title']?></td>
                    <td><?=$answer['votes_count']?> (<?=$answer['percent']?>%)</td>
			    </tr>
            <?endforeach;?>
		    </tbody>
	    </table>
    <?endif;?>
</div>
<?endif;?>
<!-- [ends] .mini-poll-results // -->

