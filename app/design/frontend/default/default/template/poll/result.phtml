<?if($poll) :?>
<div class="box mini-poll">
	<div class="head"> <!-- Gives room for control buttons -->
		<h3><span>Today's Poll</span></h3>
	</div>
	<h4><?=$poll->getPollTitle();?></h4>
    <?if( $poll_answers ):?>
	    <table cellspacing="0">
		    <tbody>
            <?foreach( $poll_answers as $answer ):?>
                <tr>
                    <td><?=$answer->getAnswerTitle()?></td>
                    <td><?=$answer->getVotesCount()?> (<?=$answer->getPercent()?>%)</td>
			    </tr>
            <?endforeach;?>
		    </tbody>
	    </table>
    <?endif;?>
</div>
<?endif;?>
<!-- [ends] .mini-poll-results // -->

