<?php
/**
 * {license_notice}
 *
 * @category    design
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<?php if(isset($poll)) : ?>
<div class="block poll">
    <div class="title">
        <strong><?php echo __('Community Poll') ?></strong>
    </div>
    <div class="content">
        <strong class="subtitle"><?php echo $this->escapeHtml($poll->getPollTitle()); ?></strong>
        <?php if( $poll_answers ): ?>
        <ol class="items">
            <?php foreach( $poll_answers as $answer ): ?>
                <li class="item">
                    <div class="poll answer"><?php echo $this->escapeHtml($answer->getAnswerTitle()) ?></div>
                    <div class="poll result">
                        <span class="poll visual" style="width:<?php echo $answer->getPercent() ?>%"></span>
                        <span class="poll percent"><?php echo $answer->getPercent() ?>%</span>
                        <span class="poll votes"><?php echo $answer->getVotesCount() ?></span>
                    </div>
                </li>
            <?php endforeach; ?>
        </ol>
        <?php endif; ?>
    </div>
</div>
<?php endif; ?>
