<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     magento2_reference
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<div class="order details items">
    <?php $_order = $this->getOrder() ?>

    <div class="order subtitle caption">
        <strong><?php echo $this->__('Items Ordered') ?></strong>
        <?php if ($_order->getTracksCollection()->count()) : ?>
            <a href="#"
               data-mage-init="{popupWindow: {windowURL:'<?php echo $this->helper('Mage_Shipping_Helper_Data')->getTrackingPopupUrlBySalesModel($_order) ?>',windowName:'trackorder',top:0,left:0,width:800,height:600,resizable:1,scrollbars:1}}"
               title="<?php echo $this->__('Track your order') ?>"
               class="action track">
                <span><?php echo $this->__('Track your order') ?></span>
            </a>
        <?php endif; ?>
    </div>

    <?php echo $this->getChildHtml('order_items') ?>

    <?php if($this->helper('Mage_GiftMessage_Helper_Message')->getIsMessagesAvailable('order', $_order) && $_order->getGiftMessageId()): ?>
    <div class="order additional details gift">
        <h2 class="order subtitle"><?php echo $this->__('Gift Message for This Order') ?></h2>
        <?php $_giftMessage=$this->helper('Mage_GiftMessage_Helper_Message')->getGiftMessageForEntity($_order); ?>
        <dl class="gift message">
            <dt class="gift sender"><strong class="label"><?php echo $this->__('From:') ?></strong> <?php echo $this->escapeHtml($_giftMessage->getSender()) ?></dt>
            <dt class="gift recipient"><strong class="label"><?php echo $this->__('To:') ?></strong> <?php echo $this->escapeHtml($_giftMessage->getRecipient()) ?></dt>
            <dd class="message text"><?php echo $this->helper('Mage_GiftMessage_Helper_Message')->getEscapedGiftMessage($_order) ?></dd>
        </dl>
    </div>
    <?php endif; ?>
    <?php $_history = $this->getOrder()->getVisibleStatusHistory() ?>
    <?php if (count($_history)): ?>
    <div class="order additional details comments">
        <h3 class="order subtitle"><?php echo $this->__('About Your Order') ?></h3>
        <dl class="order comments">
            <?php foreach ($_history as $_historyItem): ?>
                <dt class="comment date"><?php echo $this->formatDate($_historyItem->getCreatedAtStoreDate(), 'medium', true) ?></dt>
                <dd class="comment text"><?php echo $this->escapeHtml($_historyItem->getComment()) ?></dd>
            <?php endforeach; ?>
        </dl>
    </div>
    <?php endif; ?>
    <div class="actions">
        <div class="secondary">
            <a class="action back" href="<?php echo $this->getBackUrl() ?>">
                <span><?php echo $this->getBackTitle() ?></span>
            </a>
        </div>
    </div>
</div>
