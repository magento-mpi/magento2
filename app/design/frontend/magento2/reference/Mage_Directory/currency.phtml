<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     magento2_reference
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<?php
/**
 * Currency switcher
 *
 * @see Mage_Directory_Block_Currency
 */
?>
<?php if ($this->getCurrencyCount() > 1): ?>
<?php $currencies = $this->getCurrencies(); ?>
<?php $currentCurrencyCode = $this->getCurrentCurrencyCode(); ?>
<div class="switcher currency">
    <strong class="label"><span><?php echo $this->__('Currency') ?></span></strong>
    <div class="options">
        <strong class="language-<?php echo $this->escapeHtml(Mage::app()->getStore()->getCode()) ?>">
            <span><?php echo $this->escapeHtml($currentCurrencyCode) ?> - <?php echo @$this->escapeHtml($currencies[$currentCurrencyCode]) ?></span>
        </strong>
        <button type="button" data-toggle="dropdown" class="action switch"><span><?php echo $this->__('Change')?></span></button>
        <ul data-target="dropdown">
            <?php foreach ($currencies as $_code => $_name): ?>
                <?php if($_code != $currentCurrencyCode): ?>
                    <li class="currency-<?php echo $_code ?>">
                        <a href="<?php echo $this->getSwitchCurrencyUrl($_code) ?>"><?php echo $_code ?> - <?php echo $_name ?></a>
                    </li>
                <?php endif; ?>
            <?php endforeach; ?>
        </ul>
    </div>
</div>
<?php endif; ?>
