<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     default_default
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<script type="text/javascript">
//<![CDATA[
    document.observe('dom:loaded', function () {
        rma.setLoadAttributesUrl('<?php echo $this->getLoadAttributesUrl() ?>');
        rma.setLoadBundleUrl('<?php echo $this->getLoadBundleUrl() ?>');
        rma.setLoadOrderId('<?php echo $this->getLoadOrderId() ?>');
        $$('a[class="product_to_add"]').each(function(obj) {
            obj.observe('click', rma.showBundleItems.bind(rma));
            var ch = obj.up(1).down(1);
            ch.observe('click', rma.showBundleItems.bind(rma));
        });

        $('edit_form').insert({
            bottom: new Element('div', {id: 'details_container'})
        });
    });
//]]>
</script>
