<?php
/**
 * {license_notice}
 *
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>

<?php if (count($this->getItems()) > 0): ?>
<form action="<?php echo $this->getActionUpdateUrl() ?>"
      method="post"
      id="gift-registry-items-form"
      class="form giftregistry items"
      data-mage-init='{"validation":{}}'>
    <fieldset class="fieldset">
        <?php echo $this->getBlockHtml('formkey')?>
        <div class="table-wrapper giftregistry">
            <table id="shopping-cart-table" class="data table table-giftregistry-items">
                <caption class="table-caption"><?php echo __('Gift Registry') ?></caption>
                <thead>
                    <tr>
                        <th scope="col" class="col product"><?php echo __('Product') ?></th>
                        <th scope="col" class="col note"><?php echo __('Note') ?></th>
                        <th scope="col" class="col date"><?php echo __('Added On') ?></th>
                        <th scope="col" class="col qty"><?php echo __('Qty') ?></th>
                        <th scope="col" class="col fulfilled"><?php echo __('Qty Fulfilled') ?></th>
                        <th scope="col" class="col actions"><?php echo __('Delete') ?></th>
                    </tr>
                </thead>
                <tbody>
                <?php foreach($this->getItems() as $_item): ?>
                    <?php echo $this->getItemHtml($_item) ?>
                <?php endforeach ?>
                </tbody>
            </table>
        </div>
        <div class="actions-toolbar">
            <div class="primary">
                <button type="submit" class="action primary update">
                    <span><?php echo __('Update Gift Registry') ?></span>
                </button>
            </div>
            <div class="secondary">
                <a href="<?php echo $this->escapeUrl($this->getBackUrl()) ?>" class="action back"><span><?php echo __('Back') ?></span></a>
            </div>
        </div>
    </fieldset>
</form>
<?php else: ?>
    <div class="message info empty"><span><?php echo __('This gift registry has no items.') ?></span></div>
    <div class="actions-toolbar">
        <div class="secondary">
            <a href="<?php echo $this->escapeUrl($this->getBackUrl()) ?>" class="action back"><span><?php echo __('Back') ?></span></a>
        </div>
    </div>
<?php endif ?>