<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     default_default
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<?php
    /** @var $previewModel Mage_XmlConnect_Model_Preview_Android */
    $previewModel = Mage::helper('Mage_XmlConnect_Helper_Data')->getPreviewModel();
?>
<?php if (($buttons = $previewModel->getTabItems()) && is_array($buttons) && ($buttonsCount = count($buttons))) :?>
            <?php foreach ($buttons as $button) : ?>
                <?php if ($button['action'] == 'Account') :?>
                <div class="icon icon-account">B</div>
                <?php endif; ?>
                <?php if ($button['action'] == 'Search') :?>
                <div class="icon icon-search">C</div>
                <?php endif; ?>
            <?php endforeach; ?>
<?php endif; ?>
