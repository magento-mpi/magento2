<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     default_default
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<script type="text/javascript">
//<![CDATA[
function setSettings(urlTemplate, typeElement, packageThemeElement) {
    templateSyntax = /(^|.|\r|\n)({{(\w+)}})/;
    var template = new Template(urlTemplate, templateSyntax);
    typeElement = $F(typeElement);
    packageThemeElement = $F(packageThemeElement).replace(/\//g, "-");
    if (typeElement == '') {
        alert('<?php echo Mage::helper('core')->jsQuoteEscape(Mage::helper('Mage_Widget_Helper_Data')->__('Undefined Widget Type.')); ?>');
        return false;
    }
    setLocation(template.evaluate({
        'type':    typeElement,
        'package_theme': packageThemeElement
    }));
}
function saveAndContinueEdit(){
    editForm.submit($('edit_form').action + 'back/edit/');
}
//]]>
</script>
