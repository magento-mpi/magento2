<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     enterprise_default
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<?php
    $wishlistItems = $this->getWishlistItems();
?>
<div class="page-title title-buttons">
    <?php if ($this->helper('Mage_Wishlist_Helper_Data')->isRssAllow() && count($this->getWishlistItems())): ?>
        <a href="<?php echo $this->helper('Mage_Wishlist_Helper_Data')->getRssUrl($this->getWishlistInstance()->getId()); ?>" class="link-rss"><?php echo $this->__('RSS Feed') ?></a>
    <?php endif; ?>
    <h1><?php echo $this->__('Wishlist Info') ?></h1>
</div>
<?php echo $this->getMessagesBlock()->toHtml();?>
<ul class="list-info">
    <li class="item wishlist-owner"><span class="label"><?php echo $this->__('Owner');?></span> <span class="value"><?php echo $this->escapeHtml($this->getWishlistOwner()->getName());?></span></li>
    <li class="item wishlist-name"><span class="label"><?php echo $this->__('Name');?></span> <span class="value"><?php echo $this->escapeHtml($this->getWishlistInstance()->getName());?></span></li>
    <li class="item wishlist-date"><span class="label"><?php echo $this->__('Updated at');?></span> <span class="value"><?php echo $this->formatDate($this->getWishlistInstance()->getUpdatedAt(), Mage_Core_Model_Locale::FORMAT_TYPE_MEDIUM);?></span></li>
</ul>
<h2 class="sub-title"><?php echo $this->__('Wishlist Items');?></h2>
<?php if (count($wishlistItems)): ?>
<form method="post" action="<?php echo $this->getToCartUrl();?>" id="wishlist-info-form">
    <?php $this->getChild('items')->setItems($this->getWishlistItems()); ?>
    <?php echo $this->getChildHtml('items');?>
    <div class="buttons-set buttons-set2">
        <button type="submit" title="<?php echo $this->__('Add to Cart') ?>" class="button btn-add"><span><span><?php echo $this->__('Add to Cart') ?></span></span></button>
    </div>
</form>
<div class="buttons-set">
    <p class="back-link"><a href="<?php echo $this->getBackUrl() ?>"><small>&laquo; </small><?php echo $this->__('Back') ?></a></p>
</div>
<script type="text/javascript">
//<![CDATA[
    (function() {
        var form = $('wishlist-info-form');
        var itemsSelected = function() {
            var selected = false;
            $(form).select('input.select').each(function(item) {
                if ($(item).checked) {
                    selected = true;
                }
            });
            return selected;
        };
        var validation = new Validation(form, {
            onFormValidate : function(result, elm) {
                form.isValid = result;
            }
        });
        Event.observe(form, 'submit', function(event) {
            if (form.isValid && !itemsSelected()) {
                Event.stop(event);
                alert('<?php echo $this->__('You must select items to add to cart');?>');
            }
        });
    })();
//]]>
</script>
<?php else: ?>
    <p class="wishlist-empty"><?php echo $this->__('This Wishlist is empty');?></p>
<?php endif; ?>
