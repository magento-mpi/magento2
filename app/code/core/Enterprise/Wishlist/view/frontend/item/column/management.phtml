<?php
/**
 * {license_notice}
 *
 * @category    design
 * @package     enterprise_default
 * @copyright   {copyright}
 * @license     {license_link}
 */
?>
<?php
/* @var $this Enterprise_Wishlist_Block_Customer_Wishlist_Item_Column_Management  */
/* @var $item Mage_Wishlist_Model_Item  */
$item = $this->getItem();
?>
<div class="split-button light">
    <strong<?php if ($this->getDefaultWishlist()->getId() != $this->getCurrentWishlist()->getId()): ?> onclick="Enterprise.Wishlist.moveItemTo(<?php echo $item->getId();?>, Enterprise.Wishlist.getRowQty($(this).up('tr')), <?php echo $this->getDefaultWishlist()->getId();?>);"<?php endif;?>><span><?php echo $this->__('Move to Wishlist');?></span></strong>
    <a href="#" class="change"><?php echo $this->__('Move to:') ?></a>
    <div class="list-container">
        <ul>
            <?php foreach ($this->getWishlists() as $wishlist): ?>
                <?php if ($wishlist->getId() !== $this->getWishlistInstance()->getId()): ?>
                    <li onclick="Enterprise.Wishlist.moveItemTo(<?php echo $item->getId();?>, Enterprise.Wishlist.getRowQty($(this).up('tr')), <?php echo $wishlist->getId();?>);"><span title="<?php echo $this->escapeHtml($wishlist->getName());?>"><?php echo $this->escapeHtml($wishlist->getName());?></span></li>
                <?php endif;?>
            <?php endforeach;?>
            <?php if ($this->canCreateWishlists($this->getWishlists())): ?>
                <li class="new" onclick="Enterprise.Wishlist.moveItemToNew(<?php echo $item->getId();?>, Enterprise.Wishlist.getRowQty($(this).up('tr')));"><span title="<?php echo $this->__('Create New Wishlist')?>"><?php echo $this->__('Create New Wishlist')?></span></li>
            <?php endif;?>
        </ul>
    </div>
</div>
<div class="split-button light">
    <strong<?php if ($this->getDefaultWishlist()->getId() != $this->getCurrentWishlist()->getId()): ?> onclick="Enterprise.Wishlist.copyItemTo(<?php echo $item->getId();?>, Enterprise.Wishlist.getRowQty($(this).up('tr')), <?php echo $this->getDefaultWishlist()->getId();?>);"<?php endif;?>><span><?php echo $this->__('Copy to Wishlist');?></span></strong>
    <a href="#" class="change"><?php echo $this->__('Copy to:') ?></a>
    <div class="list-container">
        <ul>
            <?php foreach ($this->getWishlists() as $wishlist): ?>
                <?php if ($wishlist->getId() !== $this->getWishlistInstance()->getId()): ?>
                    <li onclick="Enterprise.Wishlist.copyItemTo(<?php echo $item->getId();?>, Enterprise.Wishlist.getRowQty($(this).up('tr')), <?php echo $wishlist->getId();?>);"><span title="<?php echo $this->escapeHtml($wishlist->getName());?>"><?php echo $this->escapeHtml($wishlist->getName());?></span></li>
                <?php endif; ?>
            <?php endforeach;?>
            <?php if ($this->canCreateWishlists($this->getWishlists())): ?>
                <li class="new" onclick="Enterprise.Wishlist.copyItemToNew(<?php echo $item->getId();?>, Enterprise.Wishlist.getRowQty($(this).up('tr')));"><span title="<?php echo $this->__('Create New Wishlist')?>"><?php echo $this->__('Create New Wishlist')?></span></li>
            <?php endif;?>
        </ul>
    </div>
</div>
