<div class="content">
<h1 class="page-heading">Shopping Cart</h1>
<form action="<?=$data['url']['cart']?>/index/updatePost" method="post">
    <fieldset>
    <table border=1>
        <tr><th>Remove</th><th>Product</th><th>Quantity</th><th>Price</th><th>Total</th></tr>
        <?foreach($data['cart']['items'] as $item):?>
            <tr>
                <td><input type="checkbox" value="1" name="cart[<?=$item['id']?>][remove]"></td>
                <td><?=$item['name']?></td>
                <td><input name="cart[<?=$item['id']?>][qty]" value="<?=$item['qty']?>" size=2></td>
                <td><?=$item['item_price']?></td>
                <td><?=$item['row_total']?></td>
            </tr>
        <?endforeach?>
        <?foreach($data['cart']['totals'] as $total):?>
        <tr>
            <th colspan=4><?=$total['title']?></th><td><?=$total['value']?></td>
        </tr>
        <?endforeach?>
        <tr>
            <td colspan=5>
                <input type="button" value="Continue Shopping" onclick="location.href='<?=$data['url']['base']?>'">
                <input type="submit" value="Update">
                <input type="button" value="Checkout" onclick="location.href='<?=$data['url']['checkout']?>'">
            </td>        
        </tr>
    </table>
    </fieldset>
</form>

<h2>Estimate</h2>
<form action="<?=$data['url']['cart']?>/index/estimatePost" method="post">
    <fieldset>
    Zipcode: <input > <input type="submit" value="Estimate">
    </fieldset>
</form>

<h2>Coupon discount</h2>
<form action="<?=$data['url']['cart']?>/index/couponPost" method="post">
    <fieldset>
    Coupon code: <input > <input type="submit" value="Apply">
    <fieldset>
</form>
</div>