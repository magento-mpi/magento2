<div class="head">
	<h2>Customer Page</h2>
</div>
<form action="/adminhtml/customerpage/dosearchcustomers" method="POST">
	<input type="text" name="uname" /> <input type="submit" value="find" />
</form>
<?php if (count(@$customers) > 0) { ?>
	<div>
		<?php foreach ($customers as $customer) { ?>
			<h3><?=$customer['firstname']." ".$customer['lastname'];?></h3>
			<div>
				Tags: 
				<?php if (0 == $customer['tags']['total']) { echo "No tags"; } else {
				foreach ($customer['tags']['data'] as $tag) { ?>
					<?=$tag['tag_name'];?> 
				<?php } } ?>
			</div>
			<div>
				Products: 
				<?php if (0 == count($customer['products'])) { echo "No tagged products"; } else {
				foreach ($customer['products'] as $product) { ?>
					<a href="/adminhtml/productpage/dosearchproducts/?pname=<?=$product['product_name'];?>">
						<?=$product['product_name'];?>
					</a> 	
				<?php } } ?>
			</div>
		<?php } ?>
	</div>
<?php } ?>